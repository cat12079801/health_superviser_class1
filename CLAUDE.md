# CLAUDE.md

本リポジトリで作業する際に従う規約をまとめる。グローバル設定の `~/CLAUDE.md` と併せて適用する。

## プロジェクト概要

第一種衛生管理者試験の学習を行う一問一答 Web アプリである。関係法令・労働衛生・労働生理の全5カテゴリを対象とする。素の HTML / CSS / JS と JSON データだけで完結する静的サイトとして構成し、GitHub Pages へデプロイする。任意機能として、Google ログイン時のみ Supabase に進捗を保存し端末間で同期する。詳細は [REQUIREMENTS.md](REQUIREMENTS.md) に記載する。

## 開発フロー

- 初期実装以降のコード変更は、必ず作業ブランチを作成し Pull Request の形式で行う。`main` へ直接 push してはならない。人間がレビューを行うためである。
- ブランチ名はグローバル設定の命名規則（`<type>/<short-description>`）に従う。base branch 名（main 等）をそのまま使わない。
- `.github/pull_request_template.md` が存在する場合は、そのフォーマットに沿って PR を作成する。
- コミットメッセージおよび PR 本文は、である調で端的に記載する。

## 文体

- README・問題の解説文・ドキュメント等の文章では、意図した場合を除き体言止め（文末を名詞で終える表現）を使わない。文末は動詞・述語で終える。文章の正確性を担保するためである。
  - 例: 「〜を実装。」ではなく「〜を実装した。」と書く。
  - 選択肢・表のセル・見出しなど、体言で書くことが自然な箇所は対象外とする。

## 問題データ

- 問題は `data/` 配下のカテゴリ別ファイル（1ファイル＝1カテゴリ。ファイル名は `category` の値に一致させる。例: `data/law_hazardous.json`）に分割して管理し、読み込むファイルを `data/categories.json` のマニフェストに列挙する。スキーマとファイル構成は [README.md](README.md) に記載する。
- `category` / `id` / `tags` の命名規則と全5科目への拡張方針は [docs/categories.md](docs/categories.md) に定める。新しい科目・カテゴリを追加するときはこれに従う。
- 各問題の `id` は学習履歴（localStorage）と紐づくため、既存の `id` は変更しない。
- 問題・解説は法令等を一次情報として作成したオリジナルとし、市販問題集の文面を転載しない。

### 難易度

- 新規に作成する問題の難易度は、**最低でも過去問相当**とする。過去問よりやや難しめは許容するが、過去問より易しい問題は作らない。
- 具体的には、保存年数・測定や健康診断の頻度・選任要件・数値基準など、過去問で問われる水準の知識の正誤を問う形式を基本とする。
- 五肢択一の正答位置は特定の番号に偏らせず、カテゴリ全体で分散させる。

#### 難易度メタ情報（`difficulty`）

- すべての問題は、難易度を表す `difficulty` フィールドを**必須**で持つ。値は次の3つのいずれかとする。検証スクリプト（[scripts/validate-questions.mjs](scripts/validate-questions.mjs)）でも必須・列挙値を検査する。
  - `"hard"`（難）: 過去問より難しい。複数条件の組合せ判断、紛らわしい数値・保存年数・頻度の区別、複数法令にまたがる、出題頻度の低いマイナー論点、新しい制度（化学物質の自律的管理等）。
  - `"standard"`（並）: 過去問相当。典型的な数値・要件の正誤を問う標準的な問題。判断に迷う場合の既定値とする。
  - `"easy"`（易）: 過去問より易しい。基礎的・常識的で、用語の定義をそのまま問う・明らかな誤りを1つ選ぶだけ等。
- 新規作成する問題は前項の方針（最低でも過去問相当）に従うため、原則 `"standard"` か `"hard"` を付す。`"easy"` は既存問題の分類のために用意した区分であり、新規には用いない。
- 学習状況画面では、カテゴリ別正答率の下に、この `difficulty` 別の正答率を細分化して表示する（[js/store.js](js/store.js) の `summarize` が集計し、[js/app.js](js/app.js) が描画する）。値（英語キー）と表示ラベル（難/並/易）の対応も `store.js` に集約する。

#### 文量（問題文・選択肢の文字数）

`difficulty` は**知識面の難しさだけを表す軸**である。文量はこれと独立した「本番の出題形式をどれだけ再現できているか」の軸として扱い、両者を混同しない。実測でも本番の文量と知識難易度は相関せず、最も易しいとされる労働生理の選択肢が平均42.0字、最も難しいとされる関係法令（有害業務に係るもの）が平均36.7字と、むしろ逆転しているためである。

- 文量の基準は **すべての `difficulty` に共通の下限**とする。`hard` だから長く `easy` だから短くしてよい、という対応付けはしない。
- 文量が基準に届かないことを理由に `difficulty` を下げてはならない。逆に、文量を増やしただけで `hard` を付してもならない。
- 本基準は**新規に作成する問題にのみ適用**し、既存問題へ遡及しない。既存問題の `difficulty` も本基準を理由に変更しない。`id` が学習履歴と紐づくためである。
- 一方向の関係のみ認める。後述の「前提条件つき長文設問」は `hard` の定義にある「複数条件の組合せ判断」を実現する手段であるため、この型で作成した問題には原則 `hard` を付す。逆は成り立たず、`hard` の問題が必ず長文設問である必要はない。

##### 目安となる文字数

安全衛生技術試験協会の公表問題2回分（2025年4月公表・2025年10月公表、計88問440選択肢）を実測し、その **1.15倍（1.5割増し）** を新規作成時の目安とする。本番と同じ分量では本番で余裕がなくなるため、学習用として一段重い文量で慣らす狙いである。文字数は空白を除いて数える。

| 対象 | 本番の実測値 | 目安（1.15倍） |
| --- | --- | --- |
| 問題文 | 中央値41字 / 平均65.0字 | 中央値47字前後を保つ |
| 選択肢1つ | 平均44.7字 / 中央値44字 | 平均51字とし、1問につき2つ以上を58〜80字にする |
| 1問合計（問題文＋選択肢5つ） | 平均288.4字 | 332字前後（下限320字） |

- **選択肢を短く切り詰めない**。本番は選択肢の40.7%が50字以上あり、「条件・対象・措置・数値」を1文に収めた上で、そのうち1箇所だけを誤らせる作りが標準である。目安では閾値も1.15倍とし、**58字以上の選択肢を4割**確保する。単語や句だけの選択肢を5つ並べる形式は、業種を選ばせる問題や組合せ問題など、本番にも存在する型に限って用いる。
- **前提条件つきの長文設問を混ぜる**。本番では問題文が100字を超える設問が11.4%あり、「常時◯人の労働者を使用する事業場」「ただし、◯人中には次の業務に常時従事する者がそれぞれに示す人数含まれている」のように、条件を読み解かせてから判断させる。関係法令（有害業務に係るもの）に多い。目安ではこの閾値も1.15倍とし、カテゴリごとに **115字超の設問をおおむね1割** 混ぜる。
- 文量を増やす目的は、字数そのものを埋めることではなく、判断に必要な条件を省かずに提示することである。冗長な修飾や同義の言い換えで字数を稼いではならない。

##### 計測スクリプト

文量は [scripts/measure-length.mjs](scripts/measure-length.mjs) で計測する。目安・下限の値はこのスクリプトに集約し、CLAUDE.md の記述と二重管理しない。

```
node scripts/measure-length.mjs                                        # 全問の集計を表示する
node scripts/measure-length.mjs --category physiology --last 50 --check # 末尾50問を判定する
```

- 引数なしで実行した場合は集計を表示するだけで、終了コードは常に 0 とする。既存問題は本基準の制定前に作成しており、遡って基準を満たす必要がないためである。
- `--check` を付けたときだけ下限判定を行い、下回れば終了コード 1 で終了する。**問題を追加する PR では、追加した分だけを `--last <追加数>` で絞って `--check` を通す**。既存問題を含めて実行すると平均が薄まり判定の意味がなくなる。
- 選択肢と設問の長文割合は `--check` でも警告に留め、失敗にはしない。カテゴリや論点によっては長文化が不自然になる場合があるためである。
- [scripts/validate-questions.mjs](scripts/validate-questions.mjs) には文量の検査を入れない。スキーマの検証と、遡及しない作成方針の確認とを分けるためである。

### コラム（`columns`）の追加・記述方針

解説の下部に表示する `columns` は、用語のイメージを補う補足である。次の基準に従って追加・記述する。

- **想定読者**: 一般的な高校生の語彙で理解できることを基本とする。日常で触れず、具体的にどのような物・作業かを想像しにくい用語を補足の対象とする。逆に、高校生が読んで意味が分かる一般的な語にはコラムを付けない。
- **語彙**: 専門用語・業界用語（例: 潜函工法、減圧症）をそのまま使わず、平易な言葉に言い換える。やむを得ず専門語を出す場合は、その場で意味を併記する。
- **内容**: 「それが何か」と「身近な具体例・どんな場面か」を1〜3文で示す。法令の細かい数値や手続きは解説本文に任せ、コラムは用語のイメージづくりに徹する。
- **個数**: 1問あたり任意個数とし、補足が不要な問題には付けない。1つの用語につき1コラムを目安とする。
- **文体**: 解説本文と同じく、である調で述語締めとする（[文体](#文体) の規則に従う）。`title` は「〇〇とは」のように体言で書いてよい。
